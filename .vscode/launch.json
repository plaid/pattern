{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Server",
            "type": "node",
            "request": "attach",
            "address": "localhost",
            "port": 9229,
            "protocol": "inspector",
            "restart": true,
            "localRoot": "${workspaceFolder}/server",
            "remoteRoot": "${workspaceFolder}/server"
        }
    ]
}
